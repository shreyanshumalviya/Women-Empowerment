import { UserDocumentDetails } from './user-document-details';

describe('UserDocumentDetails', () => {
  it('should create an instance', () => {
    expect(new UserDocumentDetails()).toBeTruthy();
  });
});
