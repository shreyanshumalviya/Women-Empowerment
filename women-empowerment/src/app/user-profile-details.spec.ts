import { UserProfileDetails } from './user-profile-details';

describe('UserProfileDetails', () => {
  it('should create an instance', () => {
    expect(new UserProfileDetails()).toBeTruthy();
  });
});
