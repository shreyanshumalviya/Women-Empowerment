import { UpdatePasswordDetails } from './update-password-details';

describe('UpdatePasswordDetails', () => {
  it('should create an instance', () => {
    expect(new UpdatePasswordDetails()).toBeTruthy();
  });
});
